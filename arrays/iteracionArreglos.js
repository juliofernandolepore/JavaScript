let nvoArreglo = [100, 200, 300, 400, 500, 600, 700];

for (i = 0; i < nvoArreglo.length; i++) {
    console.log(`iteracion ${i+1}, indice ${i} = valor ${nvoArreglo[i]} en indice de arreglo`)
}
/* En el arreglo coloco el iterador o contador i en el lugar del indice del 
arreglo y me arroja el valor del indice en el que itera cada ciclo. 
en la iteracion se coloca i+1 para evitar que la iteracion sea 0, porque
sino se presta a confusiones de interpretacion, ya que la iteracion es la numero 1*/