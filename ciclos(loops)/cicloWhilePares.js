//ejercicio ciclo while para encontrar numeros pares 
let repeticiones= 30, iterador = 0;

while(iterador < repeticiones){
    if (iterador % 2 == 0){
        console.log(`iterador par con valor ${iterador}`)
    }    
    iterador++
}